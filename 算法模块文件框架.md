# 耆康云盾算法模块文件框架

## 📁 完整文件结构

### 一、后端架构（Java Spring Boot）

```
ueit-ai/                               # AI算法模块
├── pom.xml                            # Maven配置
└── src/main/java/com/ueit/ai/
    │
    ├── controller/                    # 算法控制器层
    │   ├── AbnormalDetectionController.java       # 异常检测API
    │   ├── TrendAnalysisController.java          # 趋势分析API
    │   ├── RiskScoreController.java               # 风险评分API
    │   ├── FallDetectionController.java            # 跌倒检测API
    │   ├── DataQualityController.java             # 数据质量API
    │   └── AlgorithmDashboardController.java      # 算法总览Dashboard
    │
    ├── service/                       # 算法服务层
    │   ├── service/                   # 服务接口
    │   │   ├── IAbnormalDetectionService.java
    │   │   ├── ITrendAnalysisService.java
    │   │   ├── IRiskScoreService.java
    │   │   ├── IFallDetectionService.java
    │   │   ├── IDataQualityService.java
    │   │   └── IAlgorithmDashboardService.java
    │   │
    │   └── impl/                     # 服务实现
    │       ├── AbnormalDetectionServiceImpl.java
    │       ├── TrendAnalysisServiceImpl.java
    │       ├── RiskScoreServiceImpl.java
    │       ├── FallDetectionServiceImpl.java
    │       ├── DataQualityServiceImpl.java
    │       └── AlgorithmDashboardServiceImpl.java
    │
    ├── model/                        # 算法模型层
    │   ├── dto/                      # 数据传输对象
    │   │   ├── AbnormalDetectionResultDTO.java
    │   │   ├── TrendAnalysisResultDTO.java
    │   │   ├── RiskScoreResultDTO.java
    │   │   ├── FallDetectionResultDTO.java
    │   │   ├── DataQualityResultDTO.java
    │   │   └── AlgorithmDashboardDTO.java
    │   │
    │   ├── vo/                       # 视图对象
    │   │   ├── AbnormalDetectionVO.java
    │   │   ├── TrendAnalysisVO.java
    │   │   ├── RiskScoreVO.java
    │   │   ├── FallDetectionVO.java
    │   │   └── DataQualityVO.java
    │   │
    │   ├── entity/                   # 实体类（算法相关）
    │   │   ├── AbnormalRecord.java
    │   │   ├── TrendRecord.java
    │   │   ├── RiskScoreRecord.java
    │   │   └── FallAlarmRecord.java
    │   │
    │   ├── enums/                    # 枚举类
    │   │   ├── AbnormalTypeEnum.java
    │   │   ├── RiskLevelEnum.java
    │   │   ├── FallStatusEnum.java
    │   │   └── AlgorithmStatusEnum.java
    │   │
    │   └── constant/                 # 常量类
    │       ├── HealthThresholdConstant.java    # 健康阈值常量
    │       └── AlgorithmConfigConstant.java   # 算法配置常量
    │
    ├── algorithm/                    # 核心算法实现
    │   ├── abnormal/                 # 异常检测算法
    │   │   ├── ThresholdDetector.java          # 阈值检测
    │   │   ├── StatisticalDetector.java       # 统计检测
    │   │   └── AbnormalDetectionStrategy.java # 检测策略接口
    │   │
    │   ├── trend/                    # 趋势分析算法
    │   │   ├── MovingAverageAnalyzer.java      # 移动平均
    │   │   ├── LinearRegressionPredictor.java # 线性回归
    │   │   └── TrendAnalyzer.java             # 趋势分析接口
    │   │
    │   ├── risk/                     # 风险评分算法
    │   │   ├── MultiIndicatorRiskScorer.java  # 多指标评分
    │   │   ├── RiskScorer.java                 # 风险评分接口
    │   │   └── RiskRuleEngine.java            # 风险规则引擎
    │   │
    │   ├── fall/                     # 跌倒检测算法
    │   │   ├── FallAlarmValidator.java         # 跌倒告警校验
    │   │   ├── ContextAnalyzer.java            # 上下文分析
    │   │   └── FallDetectionStrategy.java      # 检测策略接口
    │   │
    │   └── data/                     # 数据质量算法
    │       ├── MissingValueFiller.java         # 缺失值填充
    │       ├── OutlierDetector.java            # 异常值检测
    │       ├── DataSmoother.java                # 数据平滑
    │       └── DataQualityStrategy.java        # 质量策略接口
    │
    ├── integration/                  # Python集成层
    │   ├── PythonScriptExecutor.java           # Python脚本执行器
    │   ├── PythonProcessManager.java           # Python进程管理
    │   └── PythonAlgorithmBridge.java         # Python算法桥接
    │
    ├── mapper/                       # MyBatis映射
    │   ├── AbnormalRecordMapper.java
    │   ├── TrendRecordMapper.java
    │   ├── RiskScoreRecordMapper.java
    │   └── FallAlarmRecordMapper.java
    │
    ├── utils/                        # 工具类
    │   ├── MathUtils.java                       # 数学工具
    │   ├── StatisticsUtils.java                  # 统计工具
    │   ├── TimeUtils.java                        # 时间工具
    │   └── DataValidationUtils.java              # 数据验证工具
    │
    └── config/                       # 配置类
        ├── AlgorithmConfig.java                  # 算法配置
        └── PythonIntegrationConfig.java          # Python集成配置
```

### 二、Python算法独立模块

```
python-algorithms/                   # Python算法模块
├── requirements.txt                 # Python依赖
├── README.md                        # 说明文档
│
├── algorithms/                      # 算法实现
│   ├── __init__.py
│   │
│   ├── abnormal_detection/          # 异常检测
│   │   ├── __init__.py
│   │   ├── threshold_detector.py    # 阈值检测
│   │   ├── statistical_detector.py # 统计检测
│   │   └── base_detector.py         # 基类
│   │
│   ├── trend_analysis/              # 趋势分析
│   │   ├── __init__.py
│   │   ├── moving_average.py        # 移动平均
│   │   ├── linear_regression.py     # 线性回归
│   │   └── base_analyzer.py         # 基类
│   │
│   ├── risk_scoring/                # 风险评分
│   │   ├── __init__.py
│   │   ├── multi_indicator.py       # 多指标评分
│   │   ├── risk_engine.py           # 风险引擎
│   │   └── base_scorer.py           # 基类
│   │
│   ├── fall_detection/              # 跌倒检测
│   │   ├── __init__.py
│   │   ├── alarm_validator.py       # 告警校验
│   │   ├── context_analyzer.py      # 上下文分析
│   │   └── base_detector.py         # 基类
│   │
│   └── data_quality/                # 数据质量
│       ├── __init__.py
│       ├── missing_value.py         # 缺失值填充
│       ├── outlier_detection.py    # 异常值检测
│       ├── data_smoothing.py        # 数据平滑
│       └── base_processor.py       # 基类
│
├── models/                          # 模型定义
│   ├── __init__.py
│   ├── result.py                    # 结果模型
│   ├── input.py                     # 输入模型
│   └── config.py                    # 配置模型
│
├── utils/                           # 工具函数
│   ├── __init__.py
│   ├── math_utils.py               # 数学工具
│   ├── data_loader.py              # 数据加载
│   └── visualization.py            # 可视化
│
└── api/                            # API接口（供Java调用）
    ├── __init__.py
    ├── abnormal_api.py             # 异常检测API
    ├── trend_api.py                # 趋势分析API
    ├── risk_api.py                 # 风险评分API
    ├── fall_api.py                 # 跌倒检测API
    └── data_quality_api.py         # 数据质量API
```

### 三、前端架构（Vue3）

```
RuoYi-Vue3-Modern/src/
│
├── api/                            # API接口
│   ├── ai/                         # AI算法相关API
│   │   ├── abnormalDetection.js     # 异常检测API
│   │   ├── trendAnalysis.js        # 趋势分析API
│   │   ├── riskScore.js            # 风险评分API
│   │   ├── fallDetection.js        # 跌倒检测API
│   │   ├── dataQuality.js          # 数据质量API
│   │   └── algorithmDashboard.js   # 算法总览API
│   │
│   └── health/                     # 健康数据API（已存在）
│
├── views/                          # 页面视图
│   └── ai/                         # AI算法页面
│       ├── dashboard/              # 算法总览
│       │   └── index.vue
│       │
│       ├── abnormal/                # 异常检测
│       │   ├── index.vue           # 异常列表
│       │   ├── detail.vue          # 异常详情
│       │   └── statistics.vue      # 统计分析
│       │
│       ├── trend/                  # 趋势分析
│       │   ├── index.vue           # 趋势总览
│       │   ├── chart.vue           # 趋势图表
│       │   └── prediction.vue      # 趋势预测
│       │
│       ├── risk/                   # 风险评分
│       │   ├── index.vue           # 风险列表
│       │   ├── score.vue           # 评分详情
│       │   └── history.vue         # 历史趋势
│       │
│       ├── fall/                   # 跌倒检测
│       │   ├── index.vue           # 跌倒列表
│       │   ├── alarm.vue           # 告警管理
│       │   └── analysis.vue        # 误报分析
│       │
│       └── dataQuality/            # 数据质量
│           ├── index.vue           # 质量总览
│           ├── missing.vue         # 缺失分析
│           └── outlier.vue         # 异常值分析
│
├── components/                     # 公共组件
│   └── ai/                        # AI相关组件
│       ├── charts/                 # 图表组件
│       │   ├── TrendChart.vue      # 趋势图表
│       │   ├── RiskGauge.vue       # 风险仪表盘
│       │   ├── AbnormalChart.vue   # 异常统计图
│       │   └── FallTimeline.vue    # 跌倒时间线
│       │
│       ├── cards/                  # 卡片组件
│       │   ├── RiskCard.vue        # 风险卡片
│       │   ├── MetricCard.vue      # 指标卡片
│       │   └── AlertCard.vue       # 告警卡片
│       │
│       └── tables/                 # 表格组件
│           ├── AbnormalTable.vue   # 异常表格
│           ├── TrendTable.vue     # 趋势表格
│           └── FallTable.vue       # 跌倒表格
│
└── router/                         # 路由配置
    └── ai/                        # AI路由配置
        └── index.js                # 算法模块路由
```

## 📊 数据库设计

### 异常记录表
```sql
CREATE TABLE ai_abnormal_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    device_id VARCHAR(64) COMMENT '设备ID',
    metric_type VARCHAR(32) NOT NULL COMMENT '指标类型(heart_rate/blood_pressure/temp/spo2)',
    abnormal_value VARCHAR(64) NOT NULL COMMENT '异常值',
    normal_range VARCHAR(64) COMMENT '正常范围',
    abnormal_type VARCHAR(32) COMMENT '异常类型(too_high/too_low)',
    risk_level VARCHAR(16) COMMENT '风险等级(danger/warning/normal)',
    detection_method VARCHAR(32) COMMENT '检测方法(threshold/statistical)',
    detected_time DATETIME NOT NULL COMMENT '检测时间',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='异常检测记录表';
```

### 趋势记录表
```sql
CREATE TABLE ai_trend_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    metric_type VARCHAR(32) NOT NULL COMMENT '指标类型',
    trend_direction VARCHAR(16) COMMENT '趋势方向(up/down/stable)',
    trend_strength DECIMAL(5,2) COMMENT '趋势强度',
    predicted_value DECIMAL(10,2) COMMENT '预测值',
    prediction_confidence DECIMAL(5,2) COMMENT '预测置信度',
    analysis_time DATETIME NOT NULL COMMENT '分析时间',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='趋势分析记录表';
```

### 风险评分记录表
```sql
CREATE TABLE ai_risk_score_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    total_score INT COMMENT '总评分(0-100)',
    risk_level VARCHAR(16) COMMENT '风险等级(high/medium/low/normal)',
    heart_rate_score INT COMMENT '心率评分',
    blood_pressure_score INT COMMENT '血压评分',
    blood_oxygen_score INT COMMENT '血氧评分',
    temperature_score INT COMMENT '体温评分',
    warnings JSON COMMENT '异常原因列表',
    score_time DATETIME NOT NULL COMMENT '评分时间',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='风险评分记录表';
```

### 跌倒告警记录表
```sql
CREATE TABLE ai_fall_alarm_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    original_alarm_id BIGINT COMMENT '原始告警ID',
    is_valid BOOLEAN COMMENT '是否有效告警',
    validation_reason TEXT COMMENT '校验原因',
    acceleration_peak DECIMAL(10,2) COMMENT '加速度峰值',
    has_removal_alert BOOLEAN COMMENT '1小时内是否有摘脱告警',
    recent_steps INT COMMENT '最近1小时步数',
    validation_time DATETIME NOT NULL COMMENT '校验时间',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='跌倒告警记录表';
```

### 数据质量记录表
```sql
CREATE TABLE ai_data_quality_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    metric_type VARCHAR(32) NOT NULL COMMENT '指标类型',
    missing_count INT COMMENT '缺失数量',
    missing_rate DECIMAL(5,2) COMMENT '缺失率',
    outlier_count INT COMMENT '异常值数量',
    outlier_rate DECIMAL(5,2) COMMENT '异常值率',
    fill_method VARCHAR(32) COMMENT '填充方法',
    quality_score INT COMMENT '质量评分(0-100)',
    check_time DATETIME NOT NULL COMMENT '检查时间',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='数据质量记录表';
```

## 🚀 实施步骤

### 阶段一：基础设施搭建（Day 1-2）

#### 1. 创建目录结构
```bash
# 后端目录
mkdir -p ueit-ai/src/main/java/com/ueit/ai/controller
mkdir -p ueit-ai/src/main/java/com/ueit/ai/service/service
mkdir -p ueit-ai/src/main/java/com/ueit/ai/service/impl
mkdir -p ueit-ai/src/main/java/com/ueit/ai/model/dto
mkdir -p ueit-ai/src/main/java/com/ueit/ai/model/vo
mkdir -p ueit-ai/src/main/java/com/ueit/ai/model/entity
mkdir -p ueit-ai/src/main/java/com/ueit/ai/model/enums
mkdir -p ueit-ai/src/main/java/com/ueit/ai/model/constant
mkdir -p ueit-ai/src/main/java/com/ueit/ai/algorithm/abnormal
mkdir -p ueit-ai/src/main/java/com/ueit/ai/algorithm/trend
mkdir -p ueit-ai/src/main/java/com/ueit/ai/algorithm/risk
mkdir -p ueit-ai/src/main/java/com/ueit/ai/algorithm/fall
mkdir -p ueit-ai/src/main/java/com/ueit/ai/algorithm/data
mkdir -p ueit-ai/src/main/java/com/ueit/ai/integration
mkdir -p ueit-ai/src/main/java/com/ueit/ai/mapper
mkdir -p ueit-ai/src/main/java/com/ueit/ai/utils
mkdir -p ueit-ai/src/main/java/com/ueit/ai/config

# Python目录
mkdir -p python-algorithms/algorithms/abnormal_detection
mkdir -p python-algorithms/algorithms/trend_analysis
mkdir -p python-algorithms/algorithms/risk_scoring
mkdir -p python-algorithms/algorithms/fall_detection
mkdir -p python-algorithms/algorithms/data_quality
mkdir -p python-algorithms/models
mkdir -p python-algorithms/utils
mkdir -p python-algorithms/api

# 前端目录
mkdir -p RuoYi-Vue3-Modern/src/api/ai
mkdir -p RuoYi-Vue3-Modern/src/views/ai/dashboard
mkdir -p RuoYi-Vue3-Modern/src/views/ai/abnormal
mkdir -p RuoYi-Vue3-Modern/src/views/ai/trend
mkdir -p RuoYi-Vue3-Modern/src/views/ai/risk
mkdir -p RuoYi-Vue3-Modern/src/views/ai/fall
mkdir -p RuoYi-Vue3-Modern/src/views/ai/dataQuality
mkdir -p RuoYi-Vue3-Modern/src/components/ai/charts
mkdir -p RuoYi-Vue3-Modern/src/components/ai/cards
mkdir -p RuoYi-Vue3-Modern/src/components/ai/tables
mkdir -p RuoYi-Vue3-Modern/src/router/ai
```

#### 2. 更新配置文件

**2.1 更新 `ueit-ai/pom.xml`**

```xml
<dependencies>
    <!-- 通用工具-->
    <dependency>
        <groupId>com.ueit</groupId>
        <artifactId>ueit-common</artifactId>
    </dependency>
    
    <!-- Spring AI OpenAI Starter -->
    <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-openai-spring-boot-starter</artifactId>
    </dependency>
    
    <!-- Python进程管理 -->
    <dependency>
        <groupId>org.zeroturnaround</groupId>
        <artifactId>zt-exec</artifactId>
        <version>1.12</version>
    </dependency>
    
    <!-- JSON处理 -->
    <dependency>
        <groupId>com.alibaba</groupId>
        <artifactId>fastjson</artifactId>
    </dependency>
    
    <!-- 健康数据模块依赖 -->
    <dependency>
        <groupId>com.ueit</groupId>
        <artifactId>ueit-health</artifactId>
    </dependency>
</dependencies>
```

**2.2 创建 `python-algorithms/requirements.txt`**

```txt
# 数据处理
pandas>=2.0.0
numpy>=1.24.0

# 机器学习
scikit-learn>=1.3.0

# 可视化
matplotlib>=3.7.0
seaborn>=0.12.0

# 统计分析
scipy>=1.11.0

# 数据验证
pydantic>=2.0.0

# 时间序列
statsmodels>=0.14.0

# API框架
flask>=2.3.0
```

**2.3 创建数据库表**

执行上述SQL语句创建5张表。

### 阶段二：核心算法实现（Day 3-7）

#### Week 1: 异常检测算法
- [ ] 实现 `ThresholdDetector.java`
- [ ] 实现 `StatisticalDetector.java`
- [ ] 实现 `threshold_detector.py`
- [ ] 实现 `statistical_detector.py`
- [ ] 创建 `AbnormalDetectionController.java`
- [ ] 创建 `AbnormalDetectionService.java`
- [ ] 创建前端 `abnormal/index.vue`

#### Week 2: 数据质量算法
- [ ] 实现 `MissingValueFiller.java`
- [ ] 实现 `missing_value.py`
- [ ] 创建 `DataQualityController.java`
- [ ] 创建 `DataQualityService.java`
- [ ] 创建前端 `dataQuality/index.vue`

#### Week 3: 趋势分析算法
- [ ] 实现 `MovingAverageAnalyzer.java`
- [ ] 实现 `LinearRegressionPredictor.java`
- [ ] 实现 `moving_average.py`
- [ ] 实现 `linear_regression.py`
- [ ] 创建 `TrendAnalysisController.java`
- [ ] 创建 `TrendAnalysisService.java`
- [ ] 创建前端 `trend/index.vue`

#### Week 4: 风险评分算法
- [ ] 实现 `MultiIndicatorRiskScorer.java`
- [ ] 实现 `multi_indicator.py`
- [ ] 创建 `RiskScoreController.java`
- [ ] 创建 `RiskScoreService.java`
- [ ] 创建前端 `risk/index.vue`

#### Week 5: 跌倒检测算法
- [ ] 实现 `FallAlarmValidator.java`
- [ ] 实现 `context_analyzer.py`
- [ ] 创建 `FallDetectionController.java`
- [ ] 创建 `FallDetectionService.java`
- [ ] 创建前端 `fall/index.vue`

#### Week 6: 系统集成和优化
- [ ] 创建 `AlgorithmDashboardController.java`
- [ ] 创建前端 `dashboard/index.vue`
- [ ] 创建公共组件
- [ ] 性能优化
- [ ] 测试和文档

## 📝 关键文件模板

### Controller示例

```java
package com.ueit.ai.controller;

import com.ueit.common.core.domain.AjaxResult;
import com.ueit.ai.service.IAbnormalDetectionService;
import com.ueit.ai.model.dto.AbnormalDetectionResultDTO;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/ai/abnormal")
public class AbnormalDetectionController {
    
    @Autowired
    private IAbnormalDetectionService abnormalDetectionService;
    
    /**
     * 检测异常值
     */
    @PostMapping("/detect")
    public AjaxResult detectAbnormal(@RequestBody AbnormalDetectionResultDTO dto) {
        return AjaxResult.success(abnormalDetectionService.detect(dto));
    }
    
    /**
     * 获取异常记录列表
     */
    @GetMapping("/list")
    public AjaxResult list(@RequestParam Long userId) {
        return AjaxResult.success(abnormalDetectionService.list(userId));
    }
}
```

### Service接口示例

```java
package com.ueit.ai.service;

import com.ueit.ai.model.dto.AbnormalDetectionResultDTO;
import com.ueit.ai.model.vo.AbnormalDetectionVO;
import java.util.List;

public interface IAbnormalDetectionService {
    
    /**
     * 检测异常
     */
    AbnormalDetectionVO detect(AbnormalDetectionResultDTO dto);
    
    /**
     * 获取异常列表
     */
    List<AbnormalDetectionVO> list(Long userId);
}
```

### Python API示例

```python
# python-algorithms/api/abnormal_api.py
from flask import Flask, request, jsonify
from algorithms.abnormal_detection.threshold_detector import ThresholdDetector

app = Flask(__name__)

@app.route('/api/abnormal/detect', methods=['POST'])
def detect_abnormal():
    data = request.json
    detector = ThresholdDetector()
    result = detector.detect(data)
    return jsonify(result)

if __name__ == '__main__':
    app.run(port=5000)
```

## 🎯 下一步行动

1. **立即执行**：运行创建目录的命令
2. **本周完成**：更新配置文件，创建数据库表
3. **下周开始**：实现第一个算法（异常检测）

需要我帮你创建这些文件吗？还是你想先从哪个模块开始？
