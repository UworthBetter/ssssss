# 算法大屏前端功能测试计划

## 一、测试环境准备

### 1.1 前置条件
- [ ] 后端服务已启动（端口 8098）
- [ ] 前端开发服务器已启动（端口 8080）
- [ ] 数据库中有测试数据（服务对象、心率、血压等）
- [ ] 用户已登录并具有相应权限
- [ ] 网络代理配置正确（`/dev-api` → `http://localhost:8098`）

### 1.2 浏览器环境
- [ ] Chrome (最新版)
- [ ] Edge (最新版)
- [ ] Firefox (最新版)

---

## 二、页面结构与布局测试

### 2.1 页面加载
**测试步骤：**
1. 访问算法大屏页面 `/ai/algorithm-dashboard`
2. 观察页面初始化过程

**预期结果：**
- [ ] 页面正常加载，无控制台错误
- [ ] 三栏布局正确显示（患者列表 | 可视化 | 告警列表）
- [ ] 顶部统计栏显示正常
- [ ] 患者选择器显示正常
- [ ] 所有组件正常渲染

### 2.2 响应式布局
**测试步骤：**
1. 缩放浏览器窗口
2. 测试不同屏幕尺寸（1400px、1200px、768px）

**预期结果：**
- [ ] 宽屏（>1400px）：三栏布局完整
- [ ] 中屏（1200-1400px）：雷达图和风险预测改为上下排列
- [ ] 窄屏（<1200px）：患者列表和告警列表调整为折叠模式

---

## 三、患者列表功能测试

### 3.1 患者列表加载
**测试步骤：**
1. 打开页面，观察患者列表面板
2. 检查控制台日志

**预期结果：**
- [ ] 自动加载患者列表
- [ ] 显示服务对象的基本信息（姓名、年龄、性别）
- [ ] 显示实时健康数据（心率、血压、血氧）
- [ ] 显示风险等级标识（低/中/高）
- [ ] 控制台输出："Service list initialized"

### 3.2 患者选择与切换
**测试步骤：**
1. 点击患者选择器下拉框
2. 选择不同的患者
3. 观察页面变化

**预期结果：**
- [ ] 下拉框显示所有可用患者
- [ ] 选择患者后，右侧患者卡片高亮显示
- [ ] 实时数据更新为新患者的数据
- [ ] 雷达图数据更新为新患者的健康指标
- [ ] 控制台输出："切换到患者: XXX"

### 3.3 患者卡片交互
**测试步骤：**
1. 点击左侧患者列表面板中的患者卡片
2. 观察选中状态和数据变化

**预期结果：**
- [ ] 点击卡片后，卡片高亮显示
- [ ] 选中患者状态同步更新
- [ ] 中间可视化区域数据更新

### 3.4 刷新功能
**测试步骤：**
1. 点击患者选择器中的"刷新"按钮
2. 观察数据更新

**预期结果：**
- [ ] 患者列表重新加载
- [ ] 实时数据重新获取
- [ ] 显示成功提示："数据已刷新"

---

## 四、实时数据更新测试

### 4.1 实时数据加载
**测试步骤：**
1. 选择一个患者
2. 打开浏览器控制台 Network 标签
3. 观察数据加载过程

**预期结果：**
- [ ] 并发请求实时数据和历史数据
- [ ] API 请求成功（200 状态码）
- [ ] 患者卡片显示实时健康指标
- [ ] 雷达图数据基于真实健康指标更新
- [ ] 控制台输出："已加载患者数据: XXX"

### 4.2 自动轮询更新
**测试步骤：**
1. 选中一个患者
2. 等待 5 秒
3. 观察患者卡片数据变化
4. 继续等待多个周期

**预期结果：**
- [ ] 每 5 秒自动更新一次患者数据
- [ ] 心率、血压、血氧数值实时变化
- [ ] 最后更新时间戳更新
- [ ] 风险等级根据数据动态调整

### 4.3 患者切换时数据更新
**测试步骤：**
1. 快速切换多个患者
2. 观察每个患者的数据加载

**预期结果：**
- [ ] 切换后立即加载新患者数据
- [ ] 旧患者数据清空
- [ ] 不会出现数据混淆
- [ ] 雷达图正确更新为新患者的数据

---

## 五、异常检测功能测试

### 5.1 滑动窗口可视化
**测试步骤：**
1. 观察中间"滑动窗口异常检测"面板
2. 点击播放/暂停按钮
3. 点击重置按钮

**预期结果：**
- [ ] Canvas 动画正常显示
- [ ] 心率数据曲线绘制正确
- [ ] 滑动窗口动画流畅
- [ ] 播放/暂停功能正常
- [ ] 重置后动画从头开始

### 5.2 异常值检测
**测试步骤：**
1. 观察滑动窗口动画
2. 等待检测到异常值（心率 > 120 或 < 50）
3. 观察异常高亮显示

**预期结果：**
- [ ] 异常值用红色高亮显示
- [ ] 异常统计数据增加
- [ ] 触发告警事件

### 5.3 异常告警生成
**测试步骤：**
1. 触发异常检测
2. 观察右侧告警列表面板
3. 检查全局告警 Store

**预期结果：**
- [ ] 告警添加到右侧列表
- [ ] 告警包含患者姓名、异常类型、数值、时间
- [ ] 高风险异常（> 120 bpm）标记为红色
- [ ] 患者卡片显示异常标识
- [ ] 控制台输出："检测到异常值: XXX bpm（患者: XXX）"

### 5.4 异常检测 API 调用
**测试步骤：**
1. 触发异常检测
2. 打开浏览器控制台 Network 标签
3. 查看 `/api/ai/abnormal/detect` 请求

**预期结果：**
- [ ] 请求方法为 POST
- [ ] 请求体包含：patientId、patientName、metricType、metricValue、windowData、avgValue、threshold
- [ ] API 返回 200 状态码
- [ ] 控制台输出："异常已记录到系统"

---

## 六、风险评分功能测试

### 6.1 风险预测流程
**测试步骤：**
1. 观察右下角"风险预测流程"面板
2. 点击"下一步"按钮
3. 点击"上一步"按钮

**预期结果：**
- [ ] 5 个流程步骤正确显示（数据采集、特征提取、异常检测、趋势分析、风险评分）
- [ ] 当前步骤高亮显示
- [ ] 已完成步骤显示绿色勾选
- [ ] 下一步按钮禁用状态正确
- [ ] 上一步按钮禁用状态正确

### 6.2 风险评分触发
**测试步骤：**
1. 确保已选择患者
2. 点击"下一步"直到"风险评分"步骤
3. 观察评分过程

**预期结果：**
- [ ] 风险评分步骤显示加载动画
- [ ] 控制台输出："正在计算风险评分..."
- [ ] 调用风险评分 API
- [ ] 更新风险评分分数
- [ ] 雷达图数据更新

### 6.3 风险评分 API 调用
**测试步骤：**
1. 触发风险评分
2. 打开浏览器控制台 Network 标签
3. 查看 `/api/ai/risk/assess` 请求

**预期结果：**
- [ ] 请求方法为 POST
- [ ] 请求体包含：patientId、heartRate、systolic、diastolic、spo2、temperature
- [ ] API 返回 200 状态码
- [ ] 返回数据包含风险评分分数（0-100）
- [ ] 控制台输出："风险评分完成: XX 分"

### 6.4 风险等级显示
**测试步骤：**
1. 获取不同分数的风险评分（低、中、高）
2. 观察评分颜色变化

**预期结果：**
- [ ] 低风险（0-30）：绿色显示
- [ ] 中风险（30-70）：橙色显示
- [ ] 高风险（70-100）：红色显示
- [ ] 评分单位正确显示"分"

### 6.5 无患者选择时的处理
**测试步骤：**
1. 取消选择患者（或清空 selectedPatientId）
2. 尝试触发风险评分

**预期结果：**
- [ ] 显示警告提示："请先选择患者"
- [ ] 不调用风险评分 API
- [ ] 风险评分保持原值不变

---

## 七、异常记录加载测试

### 7.1 最近异常记录加载
**测试步骤：**
1. 页面加载时观察控制台
2. 检查右侧告警列表面板
3. 打开 Network 标签

**预期结果：**
- [ ] 页面加载时自动调用 `/api/ai/abnormal/recent`
- [ ] API 返回最近的 10 条异常记录
- [ ] 异常记录显示在右侧告警列表
- [ ] 记录包含：患者姓名、异常类型、数值、时间
- [ ] 控制台输出："已加载 X 条异常记录"

### 7.2 异常记录数据映射
**测试步骤：**
1. 观察异常记录列表
2. 检查每条记录的数据完整性

**预期结果：**
- [ ] 患者姓名正确显示
- [ ] 异常类型正确映射（心率异常、血压异常等）
- [ ] 异常数值正确显示
- [ ] 时间格式正确（HH:MM:SS）
- [ ] 风险等级颜色正确（红色=high，橙色=medium，蓝色=low）

---

## 八、统计指标测试

### 8.1 顶部统计栏
**测试步骤：**
1. 观察页面顶部 4 个统计指标
2. 等待实时数据更新

**预期结果：**
- [ ] 显示 4 个指标：总检测样本、实时心率异常、血压异常统计、模型处理效率
- [ ] 指标数值基于全局告警 Store 计算
- [ ] 数值实时更新
- [ ] 趋势箭头正确显示（上升/下降）
- [ ] 单位正确显示（条、次、%）

### 8.2 异常统计计算
**测试步骤：**
1. 触发多次异常告警
2. 观察统计指标变化

**预期结果：**
- [ ] "实时心率异常"统计基于告警类型"心率"计数
- [ ] "血压异常统计"基于告警类型"血压"计数
- [ ] "总检测样本"数值增加
- [ ] 统计数据与实际告警数量一致

---

## 九、雷达图功能测试

### 9.1 雷达图渲染
**测试步骤：**
1. 观察左下角"特征雷达图"面板
2. 切换不同患者

**预期结果：**
- [ ] 雷达图正确渲染 5 个维度（心率状况、血压状况、心率稳定性、活动量、异常历史）
- [ ] 每个维度有对应的数值
- [ ] 图形颜色和样式正确
- [ ] 切换患者后，雷达图数据更新

### 9.2 雷达图数据更新
**测试步骤：**
1. 观察雷达图数据
2. 触发风险评分
3. 观察数据变化

**预期结果：**
- [ ] 风险评分后，雷达图数据更新
- [ ] 数据基于实时健康指标计算
- [ ] 心率评分：60-100 bpm = 90，其他 = 60
- [ ] 血压评分：<140/90 mmHg = 90，其他 = 60
- [ ] 异常历史：有告警 = 50，无告警 = 90

---

## 十、告警列表功能测试

### 10.1 告警列表显示
**测试步骤：**
1. 观察右侧"实时告警"列表面板
2. 检查告警列表内容

**预期结果：**
- [ ] 最多显示 15 条告警
- [ ] 告警按时间倒序排列
- [ ] 告警包含：患者姓名、异常类型、数值、时间
- [ ] 风险等级颜色正确
- [ ] 自动滚动到最新告警

### 10.2 告警自动滚动
**测试步骤：**
1. 触发多条告警
2. 观察告警列表滚动

**预期结果：**
- [ ] 新告警自动添加到顶部
- [ ] 列表自动滚动到最新告警
- [ ] 超过 15 条时，旧告警被移除

### 10.3 告警去重
**测试步骤：**
1. 快速触发相同类型的告警
2. 观察告警列表

**预期结果：**
- [ ] 30 秒内相同类型的告警不会重复添加
- [ ] 不同类型告警正常添加

---

## 十一、路由与导航测试

### 11.1 监控大屏导航
**测试步骤：**
1. 点击患者选择器右侧的"监控大屏"按钮
2. 观察路由跳转

**预期结果：**
- [ ] 成功跳转到 `/health/user/dataBoard`
- [ ] 页面正常加载
- [ ] 无控制台错误

### 11.2 查看详情导航
**测试步骤：**
1. 选择一个患者
2. 点击"查看详情"按钮
3. 观察路由跳转

**预期结果：**
- [ ] 成功跳转到 `/health/monitor/patient-detail/{patientId}`
- [ ] URL 中包含正确的患者 ID
- [ ] 患者详情页面正常加载

### 11.3 生成报告功能
**测试步骤：**
1. 选择一个患者
2. 点击"生成报告"按钮
3. 观察提示信息

**预期结果：**
- [ ] 显示加载提示："正在生成 XXX 的健康报告..."
- [ ] 1.5 秒后显示成功提示："报告生成成功！"
- [ ] （注：当前为模拟实现，TODO：替换为实际 API）

---

## 十二、错误处理测试

### 12.1 API 失败处理
**测试步骤：**
1. 模拟后端 API 失败（关闭后端服务或网络中断）
2. 尝试调用异常检测 API
3. 尝试调用风险评分 API

**预期结果：**
- [ ] 显示错误提示："异常检测失败"
- [ ] 显示错误提示："风险评分失败，请稍后重试"
- [ ] 控制台输出错误日志
- [ ] 页面不会崩溃
- [ ] 其他功能正常使用

### 12.2 网络中断处理
**测试步骤：**
1. 在加载患者数据时断开网络
2. 观察错误处理

**预期结果：**
- [ ] 控制台输出错误日志："加载患者数据失败"
- [ ] 控制台输出错误日志："加载异常记录失败"
- [ ] 显示友好的错误提示
- [ ] 页面不会崩溃

### 12.3 数据异常处理
**测试步骤：**
1. 传入空数据或不完整数据
2. 观察组件行为

**预期结果：**
- [ ] 组件不会崩溃
- [ ] 显示默认值或占位符
- [ ] 控制台输出警告日志

### 12.4 患者信息不完整处理
**测试步骤：**
1. 选择一个信息不完整的患者
2. 尝试"查看详情"
3. 尝试"生成报告"

**预期结果：**
- [ ] 显示警告提示："患者信息不完整"
- [ ] 不执行后续操作
- [ ] 不会跳转页面

---

## 十三、性能测试

### 13.1 页面加载性能
**测试步骤：**
1. 打开浏览器开发者工具 Performance 标签
2. 记录页面加载时间
3. 观察资源加载

**预期结果：**
- [ ] 首次加载时间 < 3 秒
- [ ] 组件渲染流畅
- [ ] 无内存泄漏

### 13.2 API 响应时间
**测试步骤：**
1. 打开 Network 标签
2. 触发各个 API 调用
3. 记录响应时间

**预期结果：**
- [ ] `/api/ai/abnormal/detect` 响应时间 < 1 秒
- [ ] `/api/ai/risk/assess` 响应时间 < 1 秒
- [ ] `/api/ai/abnormal/recent` 响应时间 < 1 秒
- [ ] 患者数据 API 响应时间 < 2 秒

### 13.3 实时更新性能
**测试步骤：**
1. 打开 Performance Monitor
2. 观察 5 秒轮询的性能影响
3. 观察 Canvas 动画的性能

**预期结果：**
- [ ] 轮询不会导致页面卡顿
- [ ] Canvas 动画流畅（> 30 FPS）
- [ ] CPU 使用率合理
- [ ] 内存使用稳定

### 13.4 长时间运行测试
**测试步骤：**
1. 让页面持续运行 1 小时
2. 观察内存和性能

**预期结果：**
- [ ] 无内存泄漏
- [ ] 性能稳定
- [ ] 实时更新正常工作
- [ ] 无控制台错误

---

## 十四、用户体验测试

### 14.1 加载状态
**测试步骤：**
1. 触发需要加载的操作
2. 观察 loading 状态

**预期结果：**
- [ ] API 调用时显示 loading 状态
- [ ] 按钮禁用状态正确
- [ ] 用户无法重复提交

### 14.2 提示信息
**测试步骤：**
1. 执行各种操作
2. 观察提示信息

**预期结果：**
- [ ] 成功操作显示绿色提示
- [ ] 警告操作显示黄色提示
- [ ] 错误操作显示红色提示
- [ ] 提示信息清晰易懂
- [ ] 提示信息自动消失

### 14.3 动画效果
**测试步骤：**
1. 观察各种动画效果
2. 测试流畅度

**预期结果：**
- [ ] 滑动窗口动画流畅
- [ ] 风险预测流程动画流畅
- [ ] 雷达图动画流畅
- [ ] 动画不影响性能

### 14.4 颜色与视觉
**测试步骤：**
1. 观察页面颜色搭配
2. 检查可读性

**预期结果：**
- [ ] 颜色符合暗色主题
- [ ] 文字清晰可读
- [ ] 高对比度标识正确
- [ ] 视觉层次清晰

---

## 十五、兼容性测试

### 15.1 浏览器兼容性
**测试步骤：**
1. 在 Chrome、Edge、Firefox 中测试
2. 对比功能一致性

**预期结果：**
- [ ] 所有浏览器功能一致
- [ ] 样式渲染一致
- [ ] API 调用正常
- [ ] Canvas 动画正常

### 15.2 屏幕分辨率
**测试步骤：**
1. 测试不同屏幕分辨率（1920x1080、1366x768、2560x1440）
2. 观察布局

**预期结果：**
- [ ] 布局自适应不同分辨率
- [ ] 组件不溢出
- [ ] 字体大小合适

---

## 十六、回归测试

### 16.1 功能回归
**测试步骤：**
1. 完成所有上述测试
2. 记录发现的问题
3. 修复问题后重新测试

**预期结果：**
- [ ] 所有测试用例通过
- [ ] 无新增 bug
- [ ] 性能保持稳定

---

## 测试执行说明

### 测试工具
- **浏览器**: Chrome DevTools、Edge DevTools
- **网络监控**: Network 标签
- **性能监控**: Performance 标签、Performance Monitor
- **控制台**: Console 标签

### 测试数据准备
- 至少 3 个服务对象
- 每个对象有心率、血压、血氧数据
- 部分数据包含异常值（心率 > 120 或 < 50）
- 部分数据有血压异常（> 160/100 mmHg）

### 测试记录模板
```
测试用例编号: [编号]
测试用例名称: [名称]
测试步骤:
1. [步骤 1]
2. [步骤 2]
预期结果:
- [ ] [预期 1]
- [ ] [预期 2]
实际结果:
[描述实际结果]
测试状态: ☑ 通过 / ☐ 失败
备注: [可选]
```

### 问题优先级
- **P0（严重）**: 导致功能无法使用、页面崩溃
- **P1（重要）**: 核心功能异常、数据错误
- **P2（一般）**: UI 问题、体验问题
- **P3（建议）**: 优化建议、改进意见

---

**测试计划版本**: v1.0  
**制定日期**: 2026-02-02  
**适用页面**: 算法大屏 (`/ai/algorithm-dashboard`)